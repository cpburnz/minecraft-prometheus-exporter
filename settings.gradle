pluginManagement {
	repositories {
		gradlePluginPortal()

		// Forge.
		maven {
			name = 'MinecraftForge'
			url = 'https://maven.minecraftforge.net/'
		}

		// Maven Central.
		mavenCentral()
	}
}

plugins {
	id 'org.gradle.toolchains.foojay-resolver-convention' version '0.5.0'
}
